fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2

# RAG（RAG_ENGINE=gemini/online，預設，線上 embedding，不需要 torch）
chromadb==0.5.5

# LLM（線上付費 API；xAI Grok 相容 OpenAI SDK 格式，不需要另外的套件）
anthropic==0.34.2
openai==1.51.0
google-generativeai==0.8.3

# 本地模型（provider=local）與本地 RAG 引擎（RAG_ENGINE=custom / llamaindex）用不到這份基礎依賴，
# 需要的話另外安裝 requirements-local-llm.txt，見 backend/README.md。
