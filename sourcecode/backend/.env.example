# 只有 provider=local（或線上金鑰都沒設定時的當日摘要 fallback）才會用到本地模型，
# 需要另外安裝 requirements-local-llm.txt。
# true（預設）= 使用 Qwen2.5-1.5B-Instruct（CPU 也可跑，速度較慢，不需要 bitsandbytes）
# false = 使用 Qwen2.5-7B-Instruct 4-bit 量化（需要 GPU + bitsandbytes，建議 VRAM 12GB 以上；暫時用不到就不用裝 bitsandbytes）
USE_SMALL_MODEL=true

# 前端網址，用於 CORS 設定，多個網址用逗號分隔
CORS_ORIGINS=http://localhost:5173

# RAG 檢索引擎：gemini/online（線上 Gemini embedding，不需要 torch）、
# custom（本地 sentence-transformers + 手寫檢索）、llamaindex（LlamaIndex VectorStoreIndex，本地 embedding）。
# custom / llamaindex 都需要另外安裝 requirements-local-llm.txt，見 backend/README.md「RAG 引擎切換」一節
# 留空不設定時會自動判斷：偵測到 GEMINI_API_KEY（或 llm_keys.json 的 google.api_key，GCP 部署會設定）
# 就用 gemini；本機沒有 key 時自動退回 llamaindex。要強制指定就取消註解下面這行。
# RAG_ENGINE=gemini
